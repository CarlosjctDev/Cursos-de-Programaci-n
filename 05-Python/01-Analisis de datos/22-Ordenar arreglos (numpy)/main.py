# Numpy  pip install numpy
# Ordenar arreglos
"""
    import numpy as np                          estandar
    a=np.random.randint(0,10,20)                Arreglo de 10 numeros aleatorios de 0 a 20
    a.sort()                                    Ordena el arreglo de forma ascendente (modifica el arreglo)
    a=a[::-1]                                   Ordenar el arreglo de forma inversa
    b=np.random.randint(0,10,20)                Arreglo de 10 numeros aleatorios de 0 a 20
    np.sort(b)                                  Ordena el arreglo de forma ascendente (No modifica el arreglo)
    c=np.sort(b)[::-1]                          Ordenar el arreglo de forma inversa 

    """