# Numpy  pip install numpy
# Condiciones
"""
    import numpy as np                      estandar
    a=np.random.randint(0,100,50)       Arreglo de 50 numeros aleatorios de 0 a 100
    np.all(a > 50)                      Si todos nuestros elementos son mayor que 50  devuelve true o false
                                            dependiendo si cumple las condiciones
    np.any(a > 10)                      Nos permite conocer si por lo menos un elemento cumple con las condiciones.
                                            va returnar verdadero si un elemento cumple con la condicion
    """