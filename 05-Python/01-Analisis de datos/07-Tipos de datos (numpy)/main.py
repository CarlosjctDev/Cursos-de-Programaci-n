# Numpy  pip install numpy
# Tipos de datos
"""
    import numpy as np                          estandar
    np.array([1,2,3,4,5])                       recibe como argumento un listado, returna un arreglo
    a=np.array([1,2,3,4,5])                     se le puede añadir una variable
    a.dtype                                     visualiza el tipo  de dato     
    a=np.array([1,2,3,4,5], dtype=float)        cambia el tipo de dato, puede recibir de parametro float,
                                                    str,bool (en python cualquier valor que sea diferente
                                                    a 0 sera tomado como verdadero.)
    a=np.array([1,2,3,4,5], dtype=np.complex)   Almacena numeros complejos

"""